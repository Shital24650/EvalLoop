# Developer Guide

- Use `npm run check` from `evalloop/` for syntax checks.
- Use `npm run build` from `evalloop/` for the Vite production build.
- Set `OPENAI_API_KEY` on the backend or Vercel project.
- Use `node cli/evalloop.js ci "prompt"` to gate deployments by reliability threshold.
